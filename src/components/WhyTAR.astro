---
const dimensions = [
  {
    number: '01',
    title: 'The Dimension of Space',
    description: 'Every asset, rider, and event is assigned a precise coordinate. We don\'t just list items — we map their existence in a global H3-indexed grid.',
    specs: [
      { label: 'Precision',  value: '1 m²'       },
      { label: 'Indexing',   value: 'H3 Level 15' },
      { label: 'Sharding',   value: 'Native geo'  },
    ],
  },
  {
    number: '02',
    title: 'The Dimension of Time',
    description: 'Time isn\'t just a timestamp in TAR. It\'s a traversable stream. Trace records every micro-event, letting AI agents replay history and predict future spikes.',
    specs: [
      { label: 'Throughput',  value: '1.2M events/s' },
      { label: 'Resolution',  value: 'Nanoseconds'   },
      { label: 'Persistence', value: 'Zero-loss'     },
    ],
  },
  {
    number: '03',
    title: 'The Intelligence Layer',
    description: 'Where Space and Time meet, Agents are born. Autonomous intelligence that lives at the edge — deciding and acting without human dashboards.',
    specs: [
      { label: 'Inference', value: 'Local-first'   },
      { label: 'Logic',     value: 'Decentralized' },
      { label: 'Recovery',  value: 'Autonomous'    },
    ],
  },
];
---

<section class="why" id="why-tar">
  <div class="container">

    <div class="why-header" data-reveal>
      <span class="why-eyebrow">Why TAR</span>
      <div class="why-head-body">
        <h2 class="why-title">Built for<br />Space-Time Scale.</h2>
        <p class="why-sub">Traditional systems are built for the web.<br />TAR is built for the physical world.</p>
      </div>
    </div>

    {dimensions.map((d, i) => (
      <div class="dim" style={`--i: ${i}`} data-reveal>
        <div class="dim-meta">
          <span class="dim-num">{d.number}</span>
        </div>
        <div class="dim-main">
          <h3 class="dim-title">{d.title}</h3>
          <p class="dim-desc">{d.description}</p>
        </div>
        <div class="dim-specs">
          {d.specs.map(s => (
            <div class="spec-row">
              <span class="spec-label">{s.label}</span>
              <span class="spec-value">{s.value}</span>
            </div>
          ))}
        </div>
      </div>
    ))}

  </div>
</section>

<style>
/* ── Section ───────────────────────────────────── */
.why {
  background: var(--dark-bg);
  color: white;
  padding: 120px 0;
}

.container {
  max-width: 1100px;
  margin: 0 auto;
  padding: 0 40px;
}

/* ── Header ────────────────────────────────────── */
.why-header {
  display: grid;
  grid-template-columns: 220px 1fr;
  gap: 48px;
  align-items: end;
  padding-bottom: 80px;
  border-bottom: 1px solid var(--border-dark);
}

.why-eyebrow {
  font-size: 0.72rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.14em;
  color: var(--red);
}

.why-head-body {
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.why-title {
  font-size: clamp(2.5rem, 5.5vw, 4rem);
  font-weight: 700;
  letter-spacing: -0.035em;
  line-height: 1;
  color: white;
}

.why-sub {
  font-size: 1.0625rem;
  line-height: 1.65;
  color: rgba(255,255,255,0.45);
  max-width: 400px;
}

/* ── Dimensions ────────────────────────────────── */
.dim {
  display: grid;
  grid-template-columns: 220px 1fr 1fr;
  gap: 48px;
  align-items: start;
  padding: 64px 0;
  border-bottom: 1px solid var(--border-dark);
}

/* Meta */
.dim-meta { padding-top: 4px; }

.dim-num {
  font-size: 0.7rem;
  color: rgba(255,255,255,0.2);
  letter-spacing: 0.06em;
}

/* Main */
.dim-main {
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.dim-title {
  font-size: clamp(1.375rem, 2vw, 1.875rem);
  font-weight: 700;
  letter-spacing: -0.025em;
  line-height: 1.15;
  color: white;
}

.dim-desc {
  font-size: 0.9375rem;
  line-height: 1.72;
  color: rgba(255,255,255,0.42);
}

/* Specs */
.dim-specs {
  display: flex;
  flex-direction: column;
  border-top: 1px solid rgba(255,255,255,0.07);
}

.spec-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 14px 0;
  border-bottom: 1px solid rgba(255,255,255,0.07);
}

.spec-label {
  font-size: 0.8125rem;
  color: rgba(255,255,255,0.32);
}

.spec-value {
  font-size: 0.875rem;
  font-weight: 600;
  color: white;
  letter-spacing: -0.01em;
}

/* ── Responsive ────────────────────────────────── */
@media (max-width: 900px) {
  .why-header {
    grid-template-columns: 1fr;
    gap: 20px;
  }

  .dim {
    grid-template-columns: 80px 1fr;
    grid-template-rows: auto auto;
  }

  .dim-specs {
    grid-column: 1 / -1;
  }
}

@media (max-width: 640px) {
  .why { padding: 80px 0; }
  .container { padding: 0 24px; }

  .dim {
    grid-template-columns: 1fr;
    gap: 24px;
    padding: 48px 0;
  }

  .dim-meta { display: none; }
}
</style>
