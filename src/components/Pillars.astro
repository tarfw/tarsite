---
const pillars = [
  {
    letter: 'T',
    name: 'Trace',
    title: 'The AI Memory System',
    tagline: 'Memory that moves with the world.',
    description: 'Trace is the brain of TAR. Every item lives in three dimensions of memory.',
    dimensions: [
      {
        name: 'State',
        subtitle: 'The DNA',
        description: 'The universal blueprint. What something is.',
        examples: ['Iced Coffee recipe', 'Bike specifications', 'Medicine composition', 'Store identity'],
        trait: 'Stable. Shareable. Global.',
      },
      {
        name: 'Events',
        subtitle: 'The Presence',
        description: 'What is happening right now.',
        examples: ['A sale at T. Nagar', 'A stock drop in Velachery', 'A delivery crossing an H3 cell', 'A price update from HQ'],
        trait: 'Singular moments in time + location.',
      },
      {
        name: 'Trace',
        subtitle: 'The History',
        description: 'The episodic memory. Every movement, sale, delay, and anomaly recorded.',
        examples: ['Forecast demand before it spikes', 'Detect failures before humans notice', 'Train AI on real city behavior'],
        trait: 'This is machine memory.',
      },
    ],
  },
  {
    letter: 'A',
    name: 'Agents',
    title: 'Autonomous Intelligence',
    tagline: 'Intelligence at every coordinate.',
    description: 'Agents are TAR thinkers. Every instance can have its own AI Agent.',
    capabilities: [
      {
        name: 'Monitor',
        items: ['Temperature thresholds', 'Stock levels', 'Delivery delays', 'Network congestion'],
      },
      {
        name: 'Decide',
        items: ['Reroute deliveries around traffic', 'Switch fulfillment locations', 'Lock stock during conflicts', 'Trigger alerts before failure'],
      },
      {
        name: 'Act',
        items: ['Without dashboards', 'Without human polling', 'In milliseconds'],
      },
    ],
    closing: 'Agents do not wait for commands. They respond to reality.',
  },
  {
    letter: 'R',
    name: 'Relay',
    title: 'The Nervous System',
    tagline: 'Relay is communication, social stream, decentralized system.',
    description: 'Relay moves data between customer apps, rider apps, POS, warehouses, and AI agents.',
    connections: ['Customer apps', 'Rider apps', 'POS devices', 'Warehouses', 'Bots', 'AI agents', 'WhatsApp', 'Telegram', 'Self-hosted chat'],
    features: [
      '400,000+ pings per second',
      'High-speed WebSockets',
      'Private vRack networking',
      'Built for festivals, sales, disasters. Connect with AI agents.',
    ],
    closing: 'Relay does not sync databases. It synchronizes experience.',
  },
];
---

<section class="pillars">
  <div class="container">
    <div class="section-header">
      <h2 class="section-title">What is TAR?</h2>
      <p class="section-subtitle">
        TAR is a city-scale AI commerce and coordination platform designed for the real world:
        offline-first businesses, hyperlocal supply chains, real-time decisions, 
        millions of concurrent users, and zero tolerance for data loss or delay.
      </p>
    </div>

    {pillars.map((pillar) => (
      <div class="pillar-section">
        <div class="pillar-header">
          <span class="pillar-letter">{pillar.letter}</span>
          <div>
            <span class="pillar-name">{pillar.name}</span>
            <h3 class="pillar-title">{pillar.title}</h3>
            <p class="pillar-tagline">{pillar.tagline}</p>
          </div>
        </div>

        <p class="pillar-description">{pillar.description}</p>

        {pillar.dimensions && (
          <div class="dimensions-grid">
            {pillar.dimensions.map((dim) => (
              <div class="dimension-card">
                <h4 class="dimension-name">{dim.name}</h4>
                <span class="dimension-subtitle">{dim.subtitle}</span>
                <p class="dimension-description">{dim.description}</p>
                <ul class="dimension-examples">
                  {dim.examples.map((ex) => <li>{ex}</li>)}
                </ul>
                <span class="dimension-trait">{dim.trait}</span>
              </div>
            ))}
          </div>
        )}

        {pillar.capabilities && (
          <div class="capabilities-grid">
            {pillar.capabilities.map((cap) => (
              <div class="capability-card">
                <h4 class="capability-name">{cap.name}</h4>
                <ul class="capability-items">
                  {cap.items.map((item) => <li>{item}</li>)}
                </ul>
              </div>
            ))}
          </div>
        )}

        {pillar.connections && (
          <div class="connections-section">
            <div class="connections-grid">
              {pillar.connections.map((conn) => (
                <div class="connection-item">{conn}</div>
              ))}
            </div>
          </div>
        )}

        {pillar.features && (
          <div class="features-list">
            {pillar.features.map((feature) => (
              <div class="feature-item">
                <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
                  <path d="M16.667 5L7.5 14.167L3.333 10" stroke="#0066ff" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
                {feature}
              </div>
            ))}
          </div>
        )}

        {pillar.closing && <p class="pillar-closing">{pillar.closing}</p>}
      </div>
    ))}
  </div>
</section>

<style>
  .pillars {
    padding: 120px 0;
    background: #ffffff;
  }
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 24px;
  }
  .section-header {
    text-align: center;
    max-width: 800px;
    margin: 0 auto 80px;
  }
  .section-title {
    font-size: 2.5rem;
    font-weight: 600;
    color: #1a1a1a;
    margin-bottom: 20px;
    letter-spacing: -0.02em;
  }
  .section-subtitle {
    font-size: 1.125rem;
    color: #666;
    line-height: 1.8;
  }
  .pillar-section {
    padding: 60px 0;
    border-top: 1px solid #eaeaea;
  }
  .pillar-section:first-child {
    padding-top: 0;
    border-top: none;
  }
  .pillar-header {
    display: flex;
    align-items: flex-start;
    gap: 24px;
    margin-bottom: 32px;
  }
  .pillar-letter {
    font-size: 4rem;
    font-weight: 700;
    color: #0066ff;
    line-height: 1;
  }
  .pillar-name {
    font-size: 0.875rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: #0066ff;
    display: block;
    margin-bottom: 8px;
  }
  .pillar-title {
    font-size: 2rem;
    font-weight: 600;
    color: #1a1a1a;
    margin-bottom: 12px;
    letter-spacing: -0.01em;
  }
  .pillar-tagline {
    font-size: 1.125rem;
    color: #666;
    font-style: italic;
  }
  .pillar-description {
    font-size: 1rem;
    color: #666;
    line-height: 1.8;
    margin-bottom: 40px;
  }
  .dimensions-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
    margin-bottom: 40px;
  }
  .dimension-card {
    background: #fafafa;
    border: 1px solid #eaeaea;
    border-radius: 12px;
    padding: 28px;
  }
  .dimension-name {
    font-size: 1.25rem;
    font-weight: 600;
    color: #1a1a1a;
    margin-bottom: 4px;
  }
  .dimension-subtitle {
    font-size: 0.8125rem;
    color: #0066ff;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    display: block;
    margin-bottom: 12px;
  }
  .dimension-description {
    font-size: 0.9375rem;
    color: #666;
    line-height: 1.7;
    margin-bottom: 16px;
  }
  .dimension-examples {
    list-style: none;
    margin-bottom: 16px;
  }
  .dimension-examples li {
    font-size: 0.875rem;
    color: #444;
    padding: 6px 0;
    padding-left: 16px;
    position: relative;
  }
  .dimension-examples li::before {
    content: '•';
    position: absolute;
    left: 0;
    color: #0066ff;
  }
  .dimension-trait {
    font-size: 0.8125rem;
    color: #0066ff;
    font-weight: 500;
  }
  .capabilities-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
    margin-bottom: 40px;
  }
  .capability-card {
    background: #fafafa;
    border: 1px solid #eaeaea;
    border-radius: 12px;
    padding: 28px;
  }
  .capability-name {
    font-size: 1.125rem;
    font-weight: 600;
    color: #1a1a1a;
    margin-bottom: 16px;
  }
  .capability-items {
    list-style: none;
  }
  .capability-items li {
    font-size: 0.875rem;
    color: #444;
    padding: 8px 0;
    padding-left: 16px;
    position: relative;
  }
  .capability-items li::before {
    content: '•';
    position: absolute;
    left: 0;
    color: #0066ff;
  }
  .connections-section {
    margin-bottom: 40px;
  }
  .connections-grid {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
  }
  .connection-item {
    background: #f0f7ff;
    color: #0066ff;
    padding: 10px 18px;
    border-radius: 100px;
    font-size: 0.875rem;
    font-weight: 500;
    border: 1px solid #cce0ff;
  }
  .features-list {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 16px;
    margin-bottom: 40px;
  }
  .feature-item {
    display: flex;
    align-items: center;
    gap: 12px;
    font-size: 0.9375rem;
    color: #444;
  }
  .pillar-closing {
    font-size: 1.125rem;
    color: #1a1a1a;
    font-weight: 500;
    padding: 24px;
    background: linear-gradient(135deg, #f0f7ff 0%, #faf5ff 100%);
    border-radius: 12px;
    border-left: 4px solid #0066ff;
  }
  @media (max-width: 968px) {
    .dimensions-grid,
    .capabilities-grid {
      grid-template-columns: 1fr;
    }
    .pillar-header {
      flex-direction: column;
      gap: 16px;
    }
    .pillar-letter {
      font-size: 3rem;
    }
    .features-list {
      grid-template-columns: 1fr;
    }
  }
</style>
