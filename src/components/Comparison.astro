---
const pillars = [
  {
    title: "The Static Web",
    type: "legacy",
    subtitle: "Built for requests",
    description: "Traditional systems assume always-offline is an error state. They rely on centralized polling and human dashboards.",
    points: ["Always-online reliance", "Centralized human control", "Manual reconciliation", "Static, rigid catalogs"]
  },
  {
    title: "The Living OS",
    type: "tar",
    subtitle: "Built for physics",
    description: "TAR assumes reality is intermittent. It uses event-driven memory to synchronize state across a moving city.",
    points: ["Offline-first by default", "Autonomous AI decisioning", "Real-time event streams", "AI-native dynamic memory"]
  }
];

const highlights = [
  { title: "Cities", desc: "10M+ Concurrent Souls" },
  { title: "Memory", desc: "Episodic Trace Records" },
  { title: "Scale", desc: "Millisecond Decisions" }
];
---

<section class="comparison-modern" id="why-tar">
  <div class="glow-sphere"></div>
  
  <div class="container">
    <header class="comp-header" data-animate>
      <span class="label">Architecture</span>
      <h2 class="title">Built for <br/><span class="serif-italic">Space-Time Scale</span></h2>
    </header>

    <div class="comparison-layout">
      {pillars.map((pillar) => (
        <div class={`pillar-card ${pillar.type}`} data-animate>
          <div class="card-inner">
            <span class="pillar-subtitle">{pillar.subtitle}</span>
            <h3 class="pillar-title">{pillar.title}</h3>
            <p class="pillar-desc">{pillar.description}</p>
            
            <ul class="points-list">
              {pillar.points.map((point) => (
                <li>
                  <span class="icon">{pillar.type === 'tar' ? '✓' : '×'}</span>
                  {point}
                </li>
              ))}
            </ul>
          </div>
        </div>
      ))}
    </div>

    <div class="stats-bar" data-animate>
      {highlights.map((h) => (
        <div class="stat-item">
          <span class="stat-title">{h.title}</span>
          <span class="stat-desc">{h.desc}</span>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .comparison-modern {
    padding: 160px 0;
    background: #ffffff;
    position: relative;
    overflow: hidden;
  }

  .glow-sphere {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 600px;
    height: 600px;
    background: radial-gradient(circle, rgba(0, 102, 255, 0.15) 0%, transparent 70%);
    pointer-events: none;
    z-index: 0;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 40px;
    position: relative;
    z-index: 1;
  }

  .comp-header {
    text-align: center;
    margin-bottom: 100px;
  }

  .label {
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    color: var(--color-primary);
    margin-bottom: 24px;
    display: block;
  }

  .title {
    font-size: clamp(3.5rem, 8vw, 6rem);
    line-height: 0.95;
    letter-spacing: -0.04em;
    color: var(--color-text-main);
  }

  .serif-italic {
    font-family: var(--font-display);
    font-style: italic;
    font-weight: 400;
    color: var(--color-accent);
  }

  /* Comparison Cards */
  .comparison-layout {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 40px;
    margin-bottom: 120px;
  }

  .pillar-card {
    border-radius: 48px;
    padding: 60px;
    transition: all 0.5s cubic-bezier(0.16, 1, 0.3, 1);
    position: relative;
  }

  .pillar-card.legacy {
    background: #f9f9fb;
    border: 1px solid rgba(0,0,0,0.05);
  }

  .pillar-card.tar {
    background: #0052cc;
    background: linear-gradient(135deg, #0066ff 0%, #0044bb 100%);
    color: #ffffff;
    box-shadow: 0 40px 100px rgba(0, 102, 255, 0.2);
    border: 1px solid rgba(255, 255, 255, 0.1);
  }

  .pillar-card:hover {
    transform: translateY(-8px);
  }

  .pillar-card.tar:hover {
    box-shadow: 0 60px 120px rgba(0, 102, 255, 0.3);
  }

  .pillar-subtitle {
    font-size: 0.8125rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    margin-bottom: 32px;
    display: block;
    opacity: 0.7;
  }

  .tar .pillar-subtitle {
    color: #ffffff;
    opacity: 0.9;
  }

  .pillar-title {
    font-size: 2.75rem;
    font-weight: 600;
    margin-bottom: 24px;
    letter-spacing: -0.02em;
    line-height: 1.1;
  }

  .pillar-desc {
    font-size: 1.125rem;
    line-height: 1.7;
    margin-bottom: 48px;
  }

  .legacy .pillar-desc {
    color: #555;
  }

  .tar .pillar-desc {
    color: rgba(255, 255, 255, 0.9);
  }

  .points-list {
    list-style: none;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  .points-list li {
    font-size: 1.0625rem;
    font-weight: 500;
    display: flex;
    align-items: center;
    gap: 16px;
  }

  .icon {
    width: 24px;
    height: 24px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 14px;
    font-weight: 600;
  }

  .legacy .icon {
    background: rgba(239, 68, 68, 0.1);
    color: #ef4444;
  }

  .tar .icon {
    background: #ffffff;
    color: #0066ff;
  }

  /* Stats Bar */
  .stats-bar {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    padding: 60px;
    background: #001133;
    border-radius: 32px;
    color: #ffffff;
    border: 1px solid rgba(0, 102, 255, 0.2);
  }

  .stat-item {
    text-align: center;
    display: flex;
    flex-direction: column;
    gap: 8px;
    border-right: 1px solid rgba(255,255,255,0.1);
  }

  .stat-item:last-child {
    border-right: none;
  }

  .stat-title {
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: #00aaff;
  }

  .stat-desc {
    font-size: 1.5rem;
    font-family: var(--font-serif);
    font-weight: 400;
  }

  @media (max-width: 992px) {
    .comparison-layout {
      grid-template-columns: 1fr;
    }
    .stats-bar {
      grid-template-columns: 1fr;
      gap: 40px;
      padding: 40px;
    }
    .stat-item {
      border-right: none;
      border-bottom: 1px solid rgba(255,255,255,0.1);
      padding-bottom: 40px;
    }
    .stat-item:last-child {
      border-bottom: none;
      padding-bottom: 0;
    }
    .pillar-card {
      padding: 40px;
    }
  }
</style>
